# Copy to .env.local for local dev. In production (Vercel), set this as a
# Project Environment Variable instead — never commit the real key.
#
# Get a free key at https://resend.com after signing up with
# shomailaniazi@gmail.com (the site's contact form sends to that address
# from Resend's default onboarding@resend.dev sender, which works without
# verifying a domain as long as it's sending to the account's own inbox).
RESEND_API_KEY=
