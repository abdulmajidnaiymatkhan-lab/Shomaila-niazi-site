# Animation improvement plans

Generated by `improve-animations` (standard effort) against commit `52e86f9`.
Full audit findings and the two positive-pattern notes are in the session
that produced these; only the vetted, actionable findings became plans.

| # | Title | Severity | Category | Status |
|---|---|---|---|---|
| [001](001-press-feedback-missing-transform.md) | Make press-feedback scale actually animate on 7 CTAs | MEDIUM | Physicality & origin | DONE |
| [002](002-connect-form-success-teleport.md) | Animate the Connect form's success state in | LOW | Missed opportunities | DONE |
| [003](003-showcase-filter-teleport.md) | Cross-fade the niche-filter marquee instead of hard-swapping it | LOW | Missed opportunities | DONE |

## Recommended execution order

1. **001 first** — highest leverage (fixes a real inconsistency across 7
   locations), fully independent of the other two, zero risk of conflict.
2. **002 and 003 next, in either order** — both are independent files
   (`ConnectForm.tsx` vs. `EditShowcase.tsx`), no shared code. 003's plan
   references 002's `useGSAP`-with-`dependencies` pattern as a style
   exemplar if 002 runs first, but does not require it — 003 is
   fully self-contained regardless of order.

No plan depends on another's edits landing first; all three can also run
in parallel if using separate executor passes.
